diff --git a/src/main.jsx b/src/main.jsx
index bae1d17..547d8c7 100644
--- a/src/main.jsx
+++ b/src/main.jsx
@@ -240,6 +240,9 @@ function AccountManager({settings,onChange}){
   const add=()=>onChange({...settings,accounts:[...accounts,{id:uid(),name:`Cuenta ${accounts.length+1}`,capital:10000,type:'Challenge',currency:'USD'}]});
   const del=i=>onChange({...settings,accounts:accounts.filter((_,idx)=>idx!==i),initialBalance:accounts.filter((_,idx)=>idx!==i).reduce((s,a)=>s+Number(a.capital||0),0)||10000});
   return <div className="accountManager"><div className="accountManagerHead"><div><b>Cuentas / challenges</b><small>Configura el capital de cada cuenta para que Dashboard y Analytics calculen métricas correctas.</small></div><button type="button" className="primary compact" onClick={add}><Plus size={14}/>Agregar cuenta</button></div>{accounts.map((a,i)=><div className="accountEditRow" key={a.id||i}><input className="input" value={a.name} onChange={e=>upd(i,{name:e.target.value})} placeholder="Nombre de cuenta"/><input className="input" type="number" value={a.capital} onChange={e=>upd(i,{capital:Number(e.target.value)})} placeholder="Capital"/><select className="input" value={a.type} onChange={e=>upd(i,{type:e.target.value})}><option>Personal</option><option>Challenge</option><option>Fondeada</option><option>Demo</option></select><button type="button" className="ghost danger compact" onClick={()=>del(i)}><Trash2 size={14}/></button></div>)}</div>}
+function operationalCapital(settings={}){return normalizedAccounts(settings).reduce((sum,a)=>sum+toNumberSafe(a.capital),0)||toNumberSafe(settings.initialBalance)}
+function validOperationalPct(value,max=100){const n=toNumberSafe(value); return Number.isFinite(n)&&n>0&&n<=max}
+function isOperationalBaseReady(settings={}){return operationalCapital(settings)>0&&validOperationalPct(settings.riskPerTradePct,10)&&validOperationalPct(settings.maxDailyLossPct,100)&&validOperationalPct(settings.maxDrawdownPct,100)}
 function weekStartISO(){const base=tradingDayKey(); const d=new Date(`${base}T12:00:00`); const day=(d.getDay()+6)%7; d.setDate(d.getDate()-day); return d.toISOString().slice(0,10)}
 function evaluateRiskGuard(trades=[],settings=getRiskSettings(),initial=10000,dailyPlan=null){
   const todayStr=tradingDayKey(), weekStart=weekStartISO();
@@ -984,7 +987,7 @@ function Login({initialMode='login'}){
   </div></div>
 }
 
-function Shell({profile,tab,setTab,data,theme,toggleTheme}){const groups=[['OPERATIVA',[['dashboard','Dashboard',Home],['journal','Journal',LineChart],['checklist','Checklist',CheckCircle2],['ideas','Ideas',Lightbulb],['risk','Riesgo',SlidersHorizontal],['analytics','Analytics',BarChart3],['results','Resultados',Trophy],['brokers','Integraciones',Activity],['ecosystem','Guía',Sparkles],['emotional','Emocional',Heart]]],['WORKSPACE',[['academy','Base',BookOpen],['community','Mi espacio',Users],['announcements','Anuncios',Megaphone],['chat','Chat',MessageCircle],['online','Online',Activity]]],['RECURSOS',[['system','Sistema',Shield],['reading','Setups',BookOpen],['news','Noticias',Newspaper]]],['PERFIL',[['notifications','Notificaciones',Bell],['settings','Perfil',Settings]]]]; if(['admin','moderador'].includes(profile.role))groups.push(['ADMIN',[['admin','Admin',Shield]]]); return <aside className="side premiumSide"><div className="brand"><img className="brandLogo" src="/brand/mtc-analytics-icon.png" alt="MTC Analytics"/><div><b>MTC Analytics</b><span>Trading performance platform</span></div></div><nav>{groups.map(([group,items])=><div className="navGroup" key={group}><small>{group}</small>{items.map(([id,label,Icon])=>{const count=activityCount(data,profile,id); return <button key={id} className={`${tab===id?'active':''} ${count?'hasActivity':''}`} onClick={()=>{markNotificationsForTarget(data.notifications,id); setTab(id);}}><Icon size={16}/><span>{label}</span>{count>0&&<em>{count>9?'9+':count}</em>}</button>})}</div>)}</nav><button
+function Shell({profile,tab,setTab,data,theme,toggleTheme}){const groups=[['OPERATIVA',[['dashboard','Dashboard',Home],['journal','Journal',LineChart],['checklist','Checklist',CheckCircle2],['ideas','Ideas',Lightbulb],['risk','Riesgo',SlidersHorizontal],['analytics','Analytics',BarChart3],['results','Resultados',Trophy],['brokers','Integraciones',Activity],['ecosystem','Guía',Sparkles],['emotional','Emocional',Heart]]],['WORKSPACE',[['academy','Base',BookOpen],['community','Mi espacio',Users],['announcements','Anuncios',Megaphone],['chat','Chat',MessageCircle],['online','Online',Activity]]],['RECURSOS',[['system','Sistema',Shield],['reading','Setups',BookOpen],['news','Noticias',Newspaper]]],['PERFIL',[['notifications','Notificaciones',Bell],['settings','Base operativa',Settings]]]]; if(['admin','moderador'].includes(profile.role))groups.push(['ADMIN',[['admin','Admin',Shield]]]); return <aside className="side premiumSide"><div className="brand"><img className="brandLogo" src="/brand/mtc-analytics-icon.png" alt="MTC Analytics"/><div><b>MTC Analytics</b><span>Trading performance platform</span></div></div><nav>{groups.map(([group,items])=><div className="navGroup" key={group}><small>{group}</small>{items.map(([id,label,Icon])=>{const count=activityCount(data,profile,id); return <button key={id} className={`${tab===id?'active':''} ${count?'hasActivity':''}`} onClick={()=>{markNotificationsForTarget(data.notifications,id); setTab(id);}}><Icon size={16}/><span>{label}</span>{count>0&&<em>{count>9?'9+':count}</em>}</button>})}</div>)}</nav><button
   onClick={toggleTheme}
   className="icon"
   title={theme === 'dark' ? 'Cambiar a modo claro' : 'Cambiar a modo oscuro'}
@@ -996,9 +999,9 @@ function Shell({profile,tab,setTab,data,theme,toggleTheme}){const groups=[['OPER
   }
 </button><div className="user"><div className="avatar">{profile.avatar||profile.name?.slice(0,2)||'MT'}</div><div><b>{profile.name}</b><span>{profile.role==='admin'?'mentor/admin':'trader'}</span></div><button onClick={()=>signOut(auth)} title="Salir"><LogOut size={16}/></button></div></aside>}
 
-function MobileNav({profile,tab,setTab,data}){const items=[['dashboard','Inicio',Home],['journal','Journal',LineChart],['checklist','Checklist',CheckCircle2],['ideas','Ideas',Lightbulb],['risk','Riesgo',SlidersHorizontal],['analytics','Analytics',BarChart3],['results','Resultados',Trophy],['brokers','Integraciones',Activity],['ecosystem','Guía',Sparkles],['emotional','Emocional',Heart],['system','Sistema',Shield],['reading','Setups',BookOpen],['news','Noticias',Newspaper],['community','Mi espacio',Users],['announcements','Anuncios',Megaphone],['chat','Chat',MessageCircle],['online','Online',Activity],['notifications','Avisos',Bell],['settings','Perfil',Settings]]; if(['admin','moderador'].includes(profile.role))items.push(['admin','Admin',Shield]); return <div className="mobileNav">{items.map(([id,label,Icon])=>{const count=activityCount(data,profile,id); return <button key={id} className={`${tab===id?'active':''} ${count?'hasActivity':''}`} onClick={()=>{markNotificationsForTarget(data.notifications,id); setTab(id);}}><Icon size={18}/><span>{label}</span>{count>0&&<em>{count>9?'9+':count}</em>}</button>})}<button className="mobileLogout" onClick={()=>signOut(auth)}><LogOut size={18}/><span>Salir</span></button></div>}
+function MobileNav({profile,tab,setTab,data}){const items=[['dashboard','Inicio',Home],['journal','Journal',LineChart],['checklist','Checklist',CheckCircle2],['ideas','Ideas',Lightbulb],['risk','Riesgo',SlidersHorizontal],['analytics','Analytics',BarChart3],['results','Resultados',Trophy],['brokers','Integraciones',Activity],['ecosystem','Guía',Sparkles],['emotional','Emocional',Heart],['system','Sistema',Shield],['reading','Setups',BookOpen],['news','Noticias',Newspaper],['community','Mi espacio',Users],['announcements','Anuncios',Megaphone],['chat','Chat',MessageCircle],['online','Online',Activity],['notifications','Avisos',Bell],['settings','Base',Settings]]; if(['admin','moderador'].includes(profile.role))items.push(['admin','Admin',Shield]); return <div className="mobileNav">{items.map(([id,label,Icon])=>{const count=activityCount(data,profile,id); return <button key={id} className={`${tab===id?'active':''} ${count?'hasActivity':''}`} onClick={()=>{markNotificationsForTarget(data.notifications,id); setTab(id);}}><Icon size={18}/><span>{label}</span>{count>0&&<em>{count>9?'9+':count}</em>}</button>})}<button className="mobileLogout" onClick={()=>signOut(auth)}><LogOut size={18}/><span>Salir</span></button></div>}
 
-function Topbar({tab,profile,theme,toggleTheme}){const names={dashboard:'Dashboard principal',journal:'Journal de trading',brokers:'Integraciones',risk:'Riesgo y lotaje',checklist:'Checklist operativo',ecosystem:'Guía de plataforma',emotional:'Journal emocional',system:'Sistema',reading:'Base de setups',news:'Noticias económicas',analytics:'Analytics',academy:'Base de conocimiento',community:'Mi espacio',announcements:'Anuncios del mentor',chat:'Chat privado',online:'Usuarios online',ideas:'Ideas de trading',results:'Resultados',notifications:'Notificaciones',settings:'Perfil y configuración',admin:'Panel Admin'}; return <header className="top premiumTop"><div><h1>{names[tab]||'MTC Analytics'}</h1><p>{profile.name} · {profile.role}</p></div><div className="topActions"><button className="topCta ghost" onClick={()=>window.dispatchEvent(new CustomEvent('mtc-tab',{detail:'checklist'}))}><CheckCircle2 size={15}/> Checklist</button><button
+function Topbar({tab,profile,theme,toggleTheme}){const names={dashboard:'Dashboard principal',journal:'Journal de trading',brokers:'Integraciones',risk:'Riesgo y lotaje',checklist:'Checklist operativo',ecosystem:'Guía de plataforma',emotional:'Journal emocional',system:'Sistema',reading:'Base de setups',news:'Noticias económicas',analytics:'Analytics',academy:'Base de conocimiento',community:'Mi espacio',announcements:'Anuncios del mentor',chat:'Chat privado',online:'Usuarios online',ideas:'Ideas de trading',results:'Resultados',notifications:'Notificaciones',settings:'Base operativa',admin:'Panel Admin'}; return <header className="top premiumTop"><div><h1>{names[tab]||'MTC Analytics'}</h1><p>{profile.name} · {profile.role}</p></div><div className="topActions"><button className="topCta ghost" onClick={()=>window.dispatchEvent(new CustomEvent('mtc-tab',{detail:'checklist'}))}><CheckCircle2 size={15}/> Checklist</button><button
   className="icon"
   onClick={toggleTheme}
   title={theme === 'dark' ? 'Modo claro' : 'Modo oscuro'}
@@ -1055,7 +1058,7 @@ function ProcessSummaryCard({data,setTab}){
   return <Card title="Resumen del proceso" sub="Evidencia registrada, sin metricas ficticias." className="processSummaryCard"><div className="processSummaryGrid">{rows.map(([label,value,target])=><button key={label} onClick={()=>setTab(target)}><span>{label}</span><b>{formatMetricCard(value)}</b></button>)}</div></Card>
 }
 function StructuredOnboardingCard({data,setTab}){
-  const settingsReady=Boolean(data.settings?.initialBalance||data.settings?.accounts?.length);
+  const settingsReady=isOperationalBaseReady(data.settings);
   const dayKey=tradingDayKey();
   const todayPlan=(data.dailyPlans||[]).find(p=>(p.dayKey||p.date)===dayKey);
   const completedCount=[
@@ -2164,13 +2167,30 @@ function CoachIA({data,profile}){
 }
 
 function Notifications({data,profile,setTab}){async function read(n){await markNotificationAsRead(n.id); const target=n.target||n.type; if(target==='announcement'||target==='announcements') setTab('announcements'); else if(target==='idea'||target==='ideas') setTab('ideas'); else if(target==='academy') setTab('academy'); else if(target==='community') setTab('community'); else if(target==='chat') setTab('chat'); else toast('Notificación leída');} const ordered=[...(data.notifications||[])].sort((a,b)=>(a.read===b.read?0:a.read?1:-1)); return <main className="page"><Card title="Centro de notificaciones" sub="Tocá una notificación para ir directo a la sección correspondiente.">{ordered.map(n=><button className="notif clickable" key={n.id} onClick={()=>read(n)}><span className={n.read?'read':''}></span><div><b>{n.text}</b><p>{safeDate(n.createdAt)||n.date} · {n.type||'general'}</p></div><ChevronRight size={16}/></button>)}{!ordered.length&&<Empty title="Sin notificaciones" text="Las alertas aparecerán acá."/>}</Card></main>}
-function SettingsPage({data,profile,setProfile}){
+function LegacySettingsPage({data,profile,setProfile}){
   const [u,setU]=useState(profile),[s,setS]=useState(data.settings),[preview,setPreview]=useState(profile.photoURL||profile.photoData||'');
   function loadProfileImage(e){const f=e.target.files?.[0]; if(!f)return; if(f.size>600000){alert('Usa una imagen menor a 600 KB para esta versión. Cuando actives Storage se podrán subir imágenes grandes.');return;} const reader=new FileReader(); reader.onload=()=>{setPreview(reader.result); setU({...u,photoData:reader.result,photoURL:''});}; reader.readAsDataURL(f);}
   async function saveAll(){await setDoc(doc(db,'users',profile.uid),{...u},{merge:true}); await setDoc(doc(db,'settings',profile.uid),s,{merge:true}); setProfile(u); toast('Cambios guardados')}
   return <main className="page"><Card title="Perfil" sub="Configura cómo apareces dentro de la comunidad."><div className="profileEditor"><div className="profilePhoto">{preview?<img src={preview} alt="Foto de perfil"/>:<span>{avatarOptions.find(a=>a.id===u.avatarChoice)?.emoji||u.avatar||'MT'}</span>}</div><div><label className="ghost file futureUpload"><Camera size={16}/>Cargar foto de perfil<input type="file" accept="image/*" onChange={loadProfileImage}/></label><p className="muted">También podés elegir una miniatura animada de trading. Más adelante quedará conectado a Storage para fotos grandes.</p></div></div><div className="formGrid labeled"><Field label="Nombre visible"><input className="input" value={u.name||''} onChange={e=>setU({...u,name:e.target.value,avatar:e.target.value.slice(0,2).toUpperCase()})}/></Field><Field label="Género visual"><select className="input" value={u.gender||'masculino'} onChange={e=>setU({...u,gender:e.target.value})}>{['masculino','femenino','neutral'].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="País"><input className="input" value={u.country||''} placeholder="Argentina, México..." onChange={e=>setU({...u,country:e.target.value})}/></Field><Field label="Tipo de trader"><select className="input" value={u.type||'Day Trader'} onChange={e=>setU({...u,type:e.target.value})}>{['Day Trader','Scalper','Swing Trader','Position Trader'].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Nivel"><select className="input" value={u.level||'Inicial'} onChange={e=>setU({...u,level:e.target.value})}>{['Inicial','Intermedio','Avanzado','Fondeado','Mentor'].map(x=><option key={x}>{x}</option>)}</select></Field></div><div className="avatarPicker"><h3>Elegí sticker/miniatura de trading</h3>{avatarOptions.filter(a=>a.gender==='neutral'||a.gender===(u.gender||'masculino')).map(a=><button key={a.id} type="button" className={u.avatarChoice===a.id?'on':''} onClick={()=>setU({...u,avatarChoice:a.id,avatar:a.emoji})}><span className={`avatarMini ${a.bg}`}>{a.emoji}</span><b>{a.label}</b></button>)}</div></Card><Card title="Configuración de trading"><AccountManager settings={s} onChange={setS}/><div className="formGrid labeled"><Field label="Balance inicial"><input className="input" type="number" value={s.initialBalance} onChange={e=>setS({...s,initialBalance:Number(e.target.value)})}/></Field><Field label="Objetivo mensual %"><input className="input" type="number" value={s.monthlyGoal} onChange={e=>setS({...s,monthlyGoal:Number(e.target.value)})}/></Field><Field label="Estrategia principal"><input className="input" value={s.mainStrategy} onChange={e=>setS({...s,mainStrategy:e.target.value})}/></Field><Field label="Activos favoritos"><input className="input" value={(s.assets||[]).join(',')} onChange={e=>setS({...s,assets:e.target.value.split(',').map(x=>x.trim())})}/></Field></div><button className="primary" onClick={saveAll}>Guardar cambios</button></Card></main>}
 
 
+function SettingsPage({data,profile,setProfile}){
+  const riskStored=getRiskSettings();
+  const initialSettings={...settingsDefault,...(data.settings||{})};
+  const baseCapital=operationalCapital(initialSettings);
+  const dailyPct=initialSettings.maxDailyLossPct ?? (riskStored.maxDailyLoss&&baseCapital?Number((Number(riskStored.maxDailyLoss)/baseCapital*100).toFixed(2)):3);
+  const [u,setU]=useState(profile),[s,setS]=useState({...initialSettings,riskPerTradePct:initialSettings.riskPerTradePct ?? riskStored.riskPerTradePct,maxDrawdownPct:initialSettings.maxDrawdownPct ?? riskStored.maxDrawdownPct,maxDailyLossPct:dailyPct,mainMarket:initialSettings.mainMarket||initialSettings.assets?.[0]||'XAUUSD',preferredSession:initialSettings.preferredSession||'NY',timezone:initialSettings.timezone||Intl.DateTimeFormat().resolvedOptions().timeZone||'America/New_York'}),[preview,setPreview]=useState(profile.photoURL||profile.photoData||''),[errors,setErrors]=useState({});
+  const capital=operationalCapital(s);
+  const riskMoney=capital*toNumberSafe(s.riskPerTradePct)/100;
+  const dailyMoney=capital*toNumberSafe(s.maxDailyLossPct)/100;
+  const drawdownMoney=capital*toNumberSafe(s.maxDrawdownPct)/100;
+  const baseReady=isOperationalBaseReady(s);
+  function loadProfileImage(e){const f=e.target.files?.[0]; if(!f)return; if(f.size>600000){alert('Usa una imagen menor a 600 KB para esta version. Cuando actives Storage se podran subir imagenes grandes.');return;} const reader=new FileReader(); reader.onload=()=>{setPreview(reader.result); setU({...u,photoData:reader.result,photoURL:''});}; reader.readAsDataURL(f);}
+  function updateFirstAccountCapital(value){const accounts=normalizedAccounts(s); const nextCapital=value===''?'':Number(value); const nextAccounts=accounts.length?[{...accounts[0],capital:nextCapital},...accounts.slice(1)]:[{id:'main',name:'Cuenta principal',capital:nextCapital,type:'Personal',currency:'USD'}]; setS({...s,initialBalance:nextCapital,accounts:nextAccounts});}
+  function validateBase(){const next={}; if(operationalCapital(s)<=0)next.initialBalance='Ingresa un capital inicial mayor a 0.'; if(!validOperationalPct(s.riskPerTradePct,10))next.riskPerTradePct='Ingresa un riesgo por trade entre 0.1% y 10%.'; if(!validOperationalPct(s.maxDailyLossPct,100))next.maxDailyLossPct='Ingresa un limite diario valido entre 0.1% y 100%.'; if(!validOperationalPct(s.maxDrawdownPct,100))next.maxDrawdownPct='Ingresa un limite de drawdown valido entre 0.1% y 100%.'; if(!String(s.mainMarket||'').trim())next.mainMarket='Selecciona o escribe tu mercado principal.'; setErrors(next); return !Object.keys(next).length;}
+  async function saveAll(){if(!validateBase()){toast('Revisa los campos marcados'); return;} const cleanCapital=operationalCapital(s); const cleanAccounts=normalizedAccounts(s).map(a=>({...a,capital:toNumberSafe(a.capital)||cleanCapital})); const cleanSettings={...s,initialBalance:cleanCapital,accounts:cleanAccounts,riskPerTradePct:toNumberSafe(s.riskPerTradePct),maxDailyLossPct:toNumberSafe(s.maxDailyLossPct),maxDrawdownPct:toNumberSafe(s.maxDrawdownPct),mainMarket:String(s.mainMarket||'').trim(),mainStrategy:String(s.mainStrategy||'').trim()||'Canal de Moises',assets:Array.from(new Set([String(s.mainMarket||'').trim(),...(s.assets||[]).map(x=>String(x).trim())].filter(Boolean))),preferredSession:String(s.preferredSession||'NY'),timezone:String(s.timezone||'America/New_York')}; saveRiskSettings({...getRiskSettings(),accountCapital:cleanCapital,riskPerTradePct:cleanSettings.riskPerTradePct,maxDailyLoss:Number((cleanCapital*cleanSettings.maxDailyLossPct/100).toFixed(2)),maxDrawdownPct:cleanSettings.maxDrawdownPct}); await setDoc(doc(db,'users',profile.uid),{...u},{merge:true}); await setDoc(doc(db,'settings',profile.uid),cleanSettings,{merge:true}); setS(cleanSettings); setProfile(u); toast('Base operativa guardada')}
+  return <main className="page settingsPage"><section className="settingsHero baseOpsHero"><div><span className={`baseStatus ${baseReady?'ready':'pending'}`}>{baseReady?'Base configurada':'Base pendiente'}</span><h2>Base operativa</h2><p>Estos datos alimentan tus metricas, gestion de riesgo, dashboard y lectura de performance. Configuralos con el capital y los limites que realmente usas para operar.</p></div><div className="baseHeroStats"><div><span>Capital</span><b>{money(capital)}</b></div><div><span>Riesgo/trade</span><b>{pct(toNumberSafe(s.riskPerTradePct))}</b><small>{money(riskMoney)}</small></div><div><span>Stop diario</span><b>{pct(toNumberSafe(s.maxDailyLossPct))}</b><small>{money(dailyMoney)}</small></div></div></section><Card title="Perfil visible" sub="Datos de identidad dentro del workspace."><div className="profileEditor"><div className="profilePhoto">{preview?<img src={preview} alt="Foto de perfil"/>:<span>{avatarOptions.find(a=>a.id===u.avatarChoice)?.emoji||u.avatar||'MT'}</span>}</div><div><label className="ghost file futureUpload"><Camera size={16}/>Cargar foto de perfil<input type="file" accept="image/*" onChange={loadProfileImage}/></label><p className="muted">Tambien podes elegir una miniatura de trading. El perfil no cambia tus permisos ni tu acceso.</p></div></div><div className="formGrid labeled"><Field label="Nombre visible"><input className="input" value={u.name||''} onChange={e=>setU({...u,name:e.target.value,avatar:e.target.value.slice(0,2).toUpperCase()})}/></Field><Field label="Genero visual"><select className="input" value={u.gender||'masculino'} onChange={e=>setU({...u,gender:e.target.value})}>{['masculino','femenino','neutral'].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Pais"><input className="input" value={u.country||''} placeholder="Argentina, Mexico..." onChange={e=>setU({...u,country:e.target.value})}/></Field><Field label="Tipo de trader"><select className="input" value={u.type||'Day Trader'} onChange={e=>setU({...u,type:e.target.value})}>{['Day Trader','Scalper','Swing Trader','Position Trader'].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Nivel"><select className="input" value={u.level||'Inicial'} onChange={e=>setU({...u,level:e.target.value})}>{['Inicial','Intermedio','Avanzado','Fondeado','Mentor'].map(x=><option key={x}>{x}</option>)}</select></Field></div><div className="avatarPicker"><h3>Elegi sticker/miniatura de trading</h3>{avatarOptions.filter(a=>a.gender==='neutral'||a.gender===(u.gender||'masculino')).map(a=><button key={a.id} type="button" className={u.avatarChoice===a.id?'on':''} onClick={()=>setU({...u,avatarChoice:a.id,avatar:a.emoji})}><span className={`avatarMini ${a.bg}`}>{a.emoji}</span><b>{a.label}</b></button>)}</div></Card><Card title="Parametros de la base operativa" sub="Capital, mercado, sesion y limites que usa el dashboard para darte contexto real."><div className="baseOpsNotice"><Info size={16}/><span>Si algun dato clave falta, el onboarding mantiene este paso como pendiente.</span></div><div className="baseOpsBlock"><h3>Cuenta</h3><AccountManager settings={s} onChange={setS}/></div><div className="formGrid labeled baseOpsGrid"><Field label="Capital inicial" hint={`Base para equity, P/L y riesgo. Riesgo estimado: ${money(riskMoney)}.`}><input className={`input ${errors.initialBalance?'invalid':''}`} type="number" min="0" step="100" value={s.initialBalance??''} onChange={e=>updateFirstAccountCapital(e.target.value)}/>{errors.initialBalance&&<small className="fieldError">{errors.initialBalance}</small>}</Field><Field label="Riesgo por trade (%)" hint="Porcentaje maximo que aceptas perder por operacion."><input className={`input ${errors.riskPerTradePct?'invalid':''}`} type="number" min="0" max="10" step="0.1" value={s.riskPerTradePct??''} onChange={e=>setS({...s,riskPerTradePct:e.target.value})}/>{errors.riskPerTradePct&&<small className="fieldError">{errors.riskPerTradePct}</small>}</Field><Field label="Limite diario (%)" hint={`Hard stop diario sobre capital actual. Equivale a ${money(dailyMoney)}.`}><input className={`input ${errors.maxDailyLossPct?'invalid':''}`} type="number" min="0" max="100" step="0.1" value={s.maxDailyLossPct??''} onChange={e=>setS({...s,maxDailyLossPct:e.target.value})}/>{errors.maxDailyLossPct&&<small className="fieldError">{errors.maxDailyLossPct}</small>}</Field><Field label="Limite de drawdown (%)" hint={`Umbral maximo de retroceso. Referencia actual: ${money(drawdownMoney)}.`}><input className={`input ${errors.maxDrawdownPct?'invalid':''}`} type="number" min="0" max="100" step="0.1" value={s.maxDrawdownPct??''} onChange={e=>setS({...s,maxDrawdownPct:e.target.value})}/>{errors.maxDrawdownPct&&<small className="fieldError">{errors.maxDrawdownPct}</small>}</Field><Field label="Mercado principal" hint="Se usa como referencia para el Risk Lab y tus filtros operativos."><select className={`input ${errors.mainMarket?'invalid':''}`} value={s.mainMarket||'XAUUSD'} onChange={e=>setS({...s,mainMarket:e.target.value})}>{Object.entries(instrumentPresets).map(([key,cfg])=><option key={key} value={key}>{cfg.label}</option>)}</select>{errors.mainMarket&&<small className="fieldError">{errors.mainMarket}</small>}</Field><Field label="Tipo de trader" hint="Ayuda a contextualizar tus sesiones y tu frecuencia operativa."><select className="input" value={u.type||'Day Trader'} onChange={e=>setU({...u,type:e.target.value})}>{['Day Trader','Scalper','Swing Trader','Position Trader'].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Zona horaria" hint="Referencia para jornadas, sesiones y cierre operativo."><input className="input" value={s.timezone||''} placeholder="America/New_York" onChange={e=>setS({...s,timezone:e.target.value})}/></Field><Field label="Sesion preferida" hint="Sesiones frecuentes: Londres, NY, Asia o cierre semanal."><select className="input" value={s.preferredSession||'NY'} onChange={e=>setS({...s,preferredSession:e.target.value})}>{['Asia','Londres','NY','Londres + NY','Cierre semanal'].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Objetivo mensual %" hint="Meta personal de referencia, no altera permisos ni pricing."><input className="input" type="number" step="0.1" value={s.monthlyGoal??''} onChange={e=>setS({...s,monthlyGoal:e.target.value})}/></Field><Field label="Estrategia principal" hint="Nombre del modelo o playbook que operas."><input className="input" value={s.mainStrategy||''} onChange={e=>setS({...s,mainStrategy:e.target.value})}/></Field><Field label="Activos favoritos" hint="Separalos con coma para filtros y contexto del journal."><input className="input" value={(s.assets||[]).join(',')} onChange={e=>setS({...s,assets:e.target.value.split(',').map(x=>x.trim()).filter(Boolean)})}/></Field></div><div className="settingsSaveRow"><div><b>{baseReady?'Lista para alimentar metricas':'Faltan parametros clave'}</b><p>{baseReady?'El dashboard ya puede usar esta base operativa como contexto.':'Completa capital, riesgo por trade, limite diario y drawdown para cerrar el primer paso del onboarding.'}</p></div><button className="primary" onClick={saveAll}><CheckCircle2 size={16}/>Guardar base operativa</button></div></Card></main>}
+
 function mentorStatusLabel(status){return ({pending:'Pendiente de revisión',in_review:'En revisión',reviewed:'Revisado',approved:'Aprobado',needs_work:'Requiere corrección'})[status]||'Pendiente de revisión'}
 
 
@@ -2479,7 +2499,7 @@ function GamePlanPanel(){
 }
 function CommandPalette({open,setOpen,setTab}){
   const [q,setQ]=useState('');
-  const actions=[['dashboard','Ir al Dashboard','📊'],['journal','Nuevo trade / Journal','📝'],['emotional','Journal emocional','🤍'],['brokers','Sync Broker','🔄'],['ecosystem','Cómo usar el ecosistema','✨'],['system','Sistema de Moisés','🛡️'],['reading','Biblioteca','📚'],['academy','Academia','🎓'],['community','Comunidad','🤝'],['announcements','Anuncios del mentor','📌'],['chat','Chat privado','💬'],['online','Usuarios online','🟢'],['ideas','Ideas de trading','💡'],['analytics','Analytics','📈'],['risk','Riesgo y lotaje','🛡️'],['settings','Perfil','⚙️']].filter(a=>a[1].toLowerCase().includes(q.toLowerCase()));
+  const actions=[['dashboard','Ir al Dashboard','📊'],['journal','Nuevo trade / Journal','📝'],['emotional','Journal emocional','🤍'],['brokers','Sync Broker','🔄'],['ecosystem','Cómo usar el ecosistema','✨'],['system','Sistema de Moisés','🛡️'],['reading','Biblioteca','📚'],['academy','Academia','🎓'],['community','Comunidad','🤝'],['announcements','Anuncios del mentor','📌'],['chat','Chat privado','💬'],['online','Usuarios online','🟢'],['ideas','Ideas de trading','💡'],['analytics','Analytics','📈'],['risk','Riesgo y lotaje','🛡️'],['settings','Base operativa','⚙️']].filter(a=>a[1].toLowerCase().includes(q.toLowerCase()));
   if(!open)return null;
   return <div className="cmdOverlay" onClick={()=>setOpen(false)}><div className="cmdPalette" onClick={e=>e.stopPropagation()}><div className="cmdSearch"><span>⌘K</span><input autoFocus placeholder="Buscar sección o acción rápida..." value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Escape')setOpen(false)}}/></div><div className="cmdList">{actions.map(([id,label,ico])=><button key={id} onClick={()=>{setTab(id); setOpen(false); toast(`Abriendo ${label}`)}}><span>{ico}</span><b>{label}</b></button>)}</div></div></div>
 }
